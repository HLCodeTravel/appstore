<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:id="@+id/request_fail"
    android:layout_gravity="center"
    android:gravity="center"
    android:orientation="vertical">

    <TextView
        android:id="@+id/text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:drawablePadding="10dp"
        android:drawableTop="@drawable/appcenter_nonetwork"
        android:gravity="center"
        android:text="@string/appcenter_request_fail"
        android:textColor="#000000"
        android:layout_marginBottom="10dp"
        android:layout_gravity="center" />

    <Button
        android:id="@+id/request_fail_refresh"
        android:minWidth="80dp"
        android:layout_width="wrap_content"
        android:layout_height="30dp"
        android:text="@string/appcenter_apps_update_refresh"
        android:singleLine="true"
        android:gravity="center"
        android:textColor="#ffffff"
        android:textAllCaps="false"
        android:textSize="12sp"
        android:background="@drawable/app_detail_btn"
        android:layout_gravity="center"
        />

</LinearLayout>